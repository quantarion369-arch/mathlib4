/-
  TARGET-00A: Verify Theorem A (component-kernel formula).
  This target is self-contained and must compile without `sorry` once proofs are filled.
-/
import AQARION.Theorems
import AQARION.Proofs

-- Currently blocked by `sorry`; mark as incomplete.
#exit
